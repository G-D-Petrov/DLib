import { NextPage } from "next";

const Add: NextPage = () => {
    return (
        <div className="text-slate-200">
            <h1>Add a new book</h1>
        </div>
    );
};

export default Add;